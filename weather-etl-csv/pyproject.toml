[project]
name = "weather-etl-csv"
version = "0.1.0"
description = "ETL pipeline with weather data from Open Meteo"
readme = "README.md"
authors = [
    { name = "Marcos LM", email = "marcoslm@ciencias.unam.mx" }
]
requires-python = ">=3.14"
dependencies = [
    "adbc-driver-postgresql>=1.11.0",
    "dotenv>=0.9.9",
    "geopy>=2.4.1",
    "polars>=1.42.1",
    "pydantic>=2.13.4",
    "requests>=2.34.2",
]
