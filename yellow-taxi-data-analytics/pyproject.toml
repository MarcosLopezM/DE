[project]
name = "yellow-taxi-data-analytics"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Marcos LM", email = "marcoslm@ciencias.unam.mx" }
]
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.5",
    "pyarrow>=25.0.1",
]

[project.scripts]
yellow-taxi-data-analytics = "yellow_taxi_data_analytics:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
